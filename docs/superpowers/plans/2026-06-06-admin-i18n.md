# 后台管理多语言支持实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为后台管理系统添加完整的中英文界面支持，包括语言切换器和所有界面元素翻译

**Architecture:** 使用 React Context 管理语言状态，localStorage 持久化语言偏好，自定义 hook 提供翻译函数，不改变现有路由结构

**Tech Stack:** React Context, localStorage, TypeScript, next-intl 翻译文件

---

## 文件结构

**新增文件：**
- `src/components/admin/language-provider.tsx` - 语言状态管理 Context Provider
- `src/components/admin/language-switcher.tsx` - 语言切换器 UI 组件
- `src/hooks/use-admin-translations.ts` - 翻译 Hook

**修改文件：**
- `src/app/admin/layout.tsx` - 添加 AdminLanguageProvider
- `src/components/admin/sidebar.tsx` - 使用翻译和语言切换器
- `src/app/admin/page.tsx` - Dashboard 页面翻译
- `src/app/admin/products/page.tsx` - 产品列表页面翻译
- `src/app/admin/products/[id]/page.tsx` - 产品编辑页面翻译
- `src/app/admin/case-studies/page.tsx` - 案例研究页面翻译
- `src/app/admin/solutions/page.tsx` - 解决方案页面翻译
- `src/app/admin/inquiries/page.tsx` - 咨询页面翻译
- `messages/en/admin.json` - 扩展英文翻译
- `messages/zh/admin.json` - 扩展中文翻译

---

## Task 1: 创建语言状态管理

**Files:**
- Create: `src/components/admin/language-provider.tsx`

- [ ] **Step 1: 创建 AdminLanguageProvider 组件**

创建文件 `src/components/admin/language-provider.tsx`：

```typescript
'use client'

import { createContext, useContext, useState, useEffect } from 'react'

interface AdminLanguageContextType {
  language: 'en' | 'zh'
  setLanguage: (lang: 'en' | 'zh') => void
}

const AdminLanguageContext = createContext<AdminLanguageContextType>({
  language: 'en',
  setLanguage: () => {}
})

export function AdminLanguageProvider({ children }: { children: React.ReactNode }) {
  const [language, setLanguageState] = useState<'en' | 'zh'>('en')

  // 从 localStorage 恢复语言偏好
  useEffect(() => {
    const saved = localStorage.getItem('admin-language')
    if (saved === 'en' || saved === 'zh') {
      setLanguageState(saved)
    }
  }, [])

  // 设置语言并保存
  const setLanguage = (lang: 'en' | 'zh') => {
    setLanguageState(lang)
    localStorage.setItem('admin-language', lang)
  }

  return (
    <AdminLanguageContext.Provider value={{ language, setLanguage }}>
      {children}
    </AdminLanguageContext.Provider>
  )
}

export const useAdminLanguage = () => useContext(AdminLanguageContext)
```

- [ ] **Step 2: 验证文件创建**

检查文件是否正确创建，无语法错误。

---

## Task 2: 创建语言切换器组件

**Files:**
- Create: `src/components/admin/language-switcher.tsx`

- [ ] **Step 1: 创建 AdminLanguageSwitcher 组件**

创建文件 `src/components/admin/language-switcher.tsx`：

```typescript
'use client'

import { useAdminLanguage } from './language-provider'
import { Button } from '@/components/ui/button'

export function AdminLanguageSwitcher() {
  const { language, setLanguage } = useAdminLanguage()

  return (
    <div className="flex gap-2 p-4 border-t border-gray-800">
      <Button
        variant={language === 'en' ? 'default' : 'ghost'}
        size="sm"
        onClick={() => setLanguage('en')}
        className="flex-1"
      >
        English
      </Button>
      <Button
        variant={language === 'zh' ? 'default' : 'ghost'}
        size="sm"
        onClick={() => setLanguage('zh')}
        className="flex-1"
      >
        中文
      </Button>
    </div>
  )
}
```

- [ ] **Step 2: 验证文件创建**

检查文件是否正确创建，无语法错误。

---

## Task 3: 创建翻译 Hook

**Files:**
- Create: `src/hooks/use-admin-translations.ts`

- [ ] **Step 1: 创建 useAdminTranslations Hook**

创建文件 `src/hooks/use-admin-translations.ts`：

```typescript
'use client'

import { useAdminLanguage } from '@/components/admin/language-provider'
import adminTranslationsEn from '@/messages/en/admin.json'
import adminTranslationsZh from '@/messages/zh/admin.json'

const translations = {
  en: adminTranslationsEn,
  zh: adminTranslationsZh
}

export function useAdminTranslations() {
  const { language } = useAdminLanguage()

  return (key: string): string => {
    const keys = key.split('.')
    let value: any = translations[language]

    for (const k of keys) {
      value = value?.[k]
    }

    return value || key
  }
}
```

- [ ] **Step 2: 验证文件创建**

检查文件是否正确创建，无语法错误。

---

## Task 4: 扩展翻译文件

**Files:**
- Modify: `messages/en/admin.json`
- Modify: `messages/zh/admin.json`

- [ ] **Step 1: 扩展英文翻译文件**

修改 `messages/en/admin.json`：

```json
{
  "title": "SeekDrone Admin",
  "dashboard": "Dashboard",
  "inquiries": "Inquiries",
  "products": "Products",
  "caseStudies": "Case Studies",
  "solutions": "Solutions",
  "navigation": "Navigation",
  "footer": "Footer",
  "compliance": "Compliance",
  "emailTemplates": "Email Templates",
  "settings": "Settings",
  "media": "Media Library",
  "login": "Login",
  "logout": "Logout",
  "save": "Save",
  "cancel": "Cancel",
  "delete": "Delete",
  "edit": "Edit",
  "create": "Create",
  "published": "Published",
  "draft": "Draft",
  "loading": "Loading...",
  "saving": "Saving...",
  "add": "Add",
  "search": "Search",
  "status": "Status",
  "actions": "Actions",
  "model": "Model",
  "category": "Category",
  "featured": "Featured",
  "complianceRequired": "Compliance Required",
  "basicInfo": "Basic Info",
  "images": "Images",
  "translations": "Translations",
  "date": "Date",
  "title_field": "Title",
  "products_page": {
    "title": "Products",
    "add": "Add Product",
    "edit": "Edit Product",
    "searchPlaceholder": "Search products..."
  },
  "case_studies_page": {
    "title": "Case Studies",
    "add": "Add Case Study",
    "searchPlaceholder": "Search case studies..."
  },
  "solutions_page": {
    "title": "Solutions",
    "add": "Add Solution",
    "searchPlaceholder": "Search solutions..."
  },
  "inquiries_page": {
    "title": "Inquiries",
    "searchPlaceholder": "Search inquiries..."
  }
}
```

- [ ] **Step 2: 扩展中文翻译文件**

修改 `messages/zh/admin.json`：

```json
{
  "title": "SeekDrone 管理后台",
  "dashboard": "仪表盘",
  "inquiries": "咨询",
  "products": "产品",
  "caseStudies": "案例研究",
  "solutions": "解决方案",
  "navigation": "导航",
  "footer": "页脚",
  "compliance": "合规",
  "emailTemplates": "邮件模板",
  "settings": "设置",
  "media": "媒体库",
  "login": "登录",
  "logout": "退出",
  "save": "保存",
  "cancel": "取消",
  "delete": "删除",
  "edit": "编辑",
  "create": "创建",
  "published": "已发布",
  "draft": "草稿",
  "loading": "加载中...",
  "saving": "保存中...",
  "add": "添加",
  "search": "搜索",
  "status": "状态",
  "actions": "操作",
  "model": "型号",
  "category": "分类",
  "featured": "推荐",
  "complianceRequired": "需要合规",
  "basicInfo": "基本信息",
  "images": "图片",
  "translations": "翻译",
  "date": "日期",
  "title_field": "标题",
  "products_page": {
    "title": "产品",
    "add": "添加产品",
    "edit": "编辑产品",
    "searchPlaceholder": "搜索产品..."
  },
  "case_studies_page": {
    "title": "案例研究",
    "add": "添加案例研究",
    "searchPlaceholder": "搜索案例研究..."
  },
  "solutions_page": {
    "title": "解决方案",
    "add": "添加解决方案",
    "searchPlaceholder": "搜索解决方案..."
  },
  "inquiries_page": {
    "title": "咨询",
    "searchPlaceholder": "搜索咨询..."
  }
}
```

- [ ] **Step 3: 验证 JSON 格式**

检查 JSON 文件格式正确，无语法错误。

---

## Task 5: 修改 AdminLayout 添加 Provider

**Files:**
- Modify: `src/app/admin/layout.tsx`

- [ ] **Step 1: 修改 AdminLayout**

修改 `src/app/admin/layout.tsx`，添加 AdminLanguageProvider：

```typescript
import { redirect } from 'next/navigation'
import { headers } from 'next/headers'
import { createClient } from '@/lib/supabase/server'
import { Sidebar } from '@/components/admin/sidebar'
import { AdminLanguageProvider } from '@/components/admin/language-provider'

export default async function AdminLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const headersList = await headers()
  const pathname = headersList.get('x-pathname') || ''

  // Skip auth check for login page
  if (pathname === '/admin/login') {
    return <>{children}</>
  }

  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()

  if (!user) {
    redirect('/admin/login')
  }

  return (
    <AdminLanguageProvider>
      <div className="min-h-screen bg-gray-100">
        <Sidebar />
        <main className="lg:pl-64">
          <div className="p-6">{children}</div>
        </main>
      </div>
    </AdminLanguageProvider>
  )
}
```

- [ ] **Step 2: 验证修改**

检查文件修改正确，无语法错误。

---

## Task 6: 修改 Sidebar 使用翻译

**Files:**
- Modify: `src/components/admin/sidebar.tsx`

- [ ] **Step 1: 修改 Sidebar 组件**

修改 `src/components/admin/sidebar.tsx`，使用翻译和语言切换器：

```typescript
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import {
  LayoutDashboard,
  Mail,
  Package,
  FileText,
  Lightbulb,
  Navigation,
  LayoutGrid,
  Shield,
  MailOpen,
  Settings,
  Image,
  LogOut
} from 'lucide-react'
import { Button } from '@/components/ui/button'
import { createClient } from '@/lib/supabase/client'
import { useAdminTranslations } from '@/hooks/use-admin-translations'
import { AdminLanguageSwitcher } from './language-switcher'

export function Sidebar() {
  const pathname = usePathname()
  const t = useAdminTranslations()

  const NAV_ITEMS = [
    { href: '/admin', icon: LayoutDashboard, label: t('dashboard') },
    { href: '/admin/inquiries', icon: Mail, label: t('inquiries') },
    { href: '/admin/products', icon: Package, label: t('products') },
    { href: '/admin/case-studies', icon: FileText, label: t('caseStudies') },
    { href: '/admin/solutions', icon: Lightbulb, label: t('solutions') },
    { href: '/admin/navigation', icon: Navigation, label: t('navigation') },
    { href: '/admin/footer', icon: LayoutGrid, label: t('footer') },
    { href: '/admin/compliance', icon: Shield, label: t('compliance') },
    { href: '/admin/email-templates', icon: MailOpen, label: t('emailTemplates') },
    { href: '/admin/settings', icon: Settings, label: t('settings') },
    { href: '/admin/media', icon: Image, label: t('media') },
  ]

  async function handleLogout() {
    const supabase = createClient()
    await supabase.auth.signOut()
    window.location.href = '/admin/login'
  }

  return (
    <aside className="fixed inset-y-0 left-0 w-64 bg-gray-900 text-white hidden lg:block">
      <div className="flex flex-col h-full">
        <div className="p-6 border-b border-gray-800">
          <h1 className="text-xl font-bold">{t('title')}</h1>
        </div>
        <nav className="flex-1 p-4 space-y-1">
          {NAV_ITEMS.map((item) => {
            const isActive = pathname === item.href ||
              (item.href !== '/admin' && pathname.startsWith(item.href))
            return (
              <Link
                key={item.href}
                href={item.href}
                className={`flex items-center gap-3 px-3 py-2 rounded transition-colors ${
                  isActive ? 'bg-gray-800 text-white' : 'text-gray-400 hover:text-white hover:bg-gray-800'
                }`}
              >
                <item.icon className="w-5 h-5" />
                {item.label}
              </Link>
            )
          })}
        </nav>
        <AdminLanguageSwitcher />
        <div className="p-4 border-t border-gray-800">
          <Button
            variant="ghost"
            className="w-full justify-start text-gray-400 hover:text-white"
            onClick={handleLogout}
          >
            <LogOut className="w-5 h-5 mr-3" />
            {t('logout')}
          </Button>
        </div>
      </div>
    </aside>
  )
}
```

- [ ] **Step 2: 验证修改**

检查文件修改正确，无语法错误。

---

## Task 7: 修改 Dashboard 页面

**Files:**
- Modify: `src/app/admin/page.tsx`

- [ ] **Step 1: 修改 Dashboard 页面**

修改 `src/app/admin/page.tsx`：

```typescript
'use client'

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { supabaseAdmin } from '@/lib/supabase/admin'
import { useAdminTranslations } from '@/hooks/use-admin-translations'

export default function AdminDashboard() {
  const t = useAdminTranslations()

  // 注意：这里需要改为客户端数据获取或保持服务器组件
  // 为了简化，我们保持服务器组件，但标题使用客户端翻译
  return (
    <div>
      <h1 className="text-2xl font-bold mb-6">{t('dashboard')}</h1>
      <DashboardContent />
    </div>
  )
}

async function DashboardContent() {
  const { count: inquiryCount } = await supabaseAdmin
    .from('inquiries')
    .select('*', { count: 'exact', head: true })

  const { count: productCount } = await supabaseAdmin
    .from('products')
    .select('*', { count: 'exact', head: true })

  const { count: caseCount } = await supabaseAdmin
    .from('case_studies')
    .select('*', { count: 'exact', head: true })

  const t = useAdminTranslations()

  return (
    <div className="grid md:grid-cols-3 gap-6">
      <Card>
        <CardHeader>
          <CardTitle>{t('inquiries')}</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="text-3xl font-bold">{inquiryCount || 0}</div>
        </CardContent>
      </Card>
      <Card>
        <CardHeader>
          <CardTitle>{t('products')}</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="text-3xl font-bold">{productCount || 0}</div>
        </CardContent>
      </Card>
      <Card>
        <CardHeader>
          <CardTitle>{t('caseStudies')}</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="text-3xl font-bold">{caseCount || 0}</div>
        </CardContent>
      </Card>
    </div>
  )
}
```

- [ ] **Step 2: 验证修改**

检查文件修改正确，无语法错误。

---

## Task 8: 修改产品列表页面

**Files:**
- Modify: `src/app/admin/products/page.tsx`

- [ ] **Step 1: 修改产品列表页面**

修改 `src/app/admin/products/page.tsx`：

```typescript
'use client'

import Link from 'next/link'
import { supabaseAdmin } from '@/lib/supabase/admin'
import { DataTable } from '@/components/admin/data-table'
import { Badge } from '@/components/ui/badge'
import { buttonVariants } from '@/components/ui/button'
import { Plus } from 'lucide-react'
import { useAdminTranslations } from '@/hooks/use-admin-translations'

export default function ProductsPage() {
  const t = useAdminTranslations()

  const columns = [
    { key: 'model', label: t('model') },
    { key: 'category', label: t('category') },
    {
      key: 'published',
      label: t('status'),
      render: (item: any) => (
        <Badge variant={item.published ? 'default' : 'secondary'}>
          {item.published ? t('published') : t('draft')}
        </Badge>
      )
    },
    {
      key: 'compliance_flag',
      label: t('compliance'),
      render: (item: any) => item.compliance_flag ? <Badge variant="destructive">{t('complianceRequired')}</Badge> : 'No'
    },
    { key: 'featured', label: t('featured'), render: (item: any) => item.featured ? '⭐' : '' },
  ]

  return (
    <div>
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-bold">{t('products_page.title')}</h1>
        <Link href="/admin/products/new" className={buttonVariants()}>
          <Plus className="w-4 h-4 mr-2" />
          {t('products_page.add')}
        </Link>
      </div>
      <ProductsTable columns={columns} />
    </div>
  )
}

async function ProductsTable({ columns }: { columns: any[] }) {
  const { data: products } = await supabaseAdmin
    .from('products')
    .select('*')
    .order('sort_order')
    .order('created_at', { ascending: false })

  const t = useAdminTranslations()

  return (
    <DataTable
      data={products || []}
      columns={columns}
      searchPlaceholder={t('products_page.searchPlaceholder')}
      onRowClick={(item) => window.location.href = `/admin/products/${item.id}`}
    />
  )
}
```

- [ ] **Step 2: 验证修改**

检查文件修改正确，无语法错误。

---

## Task 9: 修改产品编辑页面

**Files:**
- Modify: `src/app/admin/products/[id]/page.tsx`

- [ ] **Step 1: 修改产品编辑页面**

修改 `src/app/admin/products/[id]/page.tsx`，添加翻译：

```typescript
'use client'

import { useState, useEffect, useCallback } from 'react'
import { useParams, useRouter } from 'next/navigation'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Switch } from '@/components/ui/switch'
import { TranslationTabs } from '@/components/admin/translation-tabs'
import { ImageUpload } from '@/components/admin/image-upload'
import { createClient } from '@/lib/supabase/client'
import { useAdminTranslations } from '@/hooks/use-admin-translations'

const TRANSLATION_FIELDS = ['name', 'overview', 'advantages', 'capabilities', 'applications']

interface ProductData {
  id?: string
  model: string
  slug: string
  category: string
  translations: Record<string, Record<string, string>>
  images: string[]
  published: boolean
  featured: boolean
  compliance_flag: boolean
}

export default function ProductEditPage() {
  const params = useParams()
  const router = useRouter()
  const t = useAdminTranslations()
  const [loading, setLoading] = useState(true)
  const [saving, setSaving] = useState(false)
  const [product, setProduct] = useState<ProductData>({
    model: '',
    slug: '',
    category: 'uav',
    translations: {},
    images: [],
    published: true,
    featured: false,
    compliance_flag: false,
  })
  const supabase = createClient()

  const fetchProduct = useCallback(async () => {
    const { data } = await supabase
      .from('products')
      .select('*')
      .eq('id', params.id)
      .single()

    if (data) {
      setProduct(data)
    }
    setLoading(false)
  }, [params.id, supabase])

  useEffect(() => {
    if (params.id !== 'new') {
      fetchProduct()
    } else {
      setLoading(false)
    }
  }, [params.id, fetchProduct])

  async function handleSave() {
    setSaving(true)
    try {
      if (params.id === 'new') {
        const { error } = await supabase
          .from('products')
          .insert([product])
        if (error) throw error
      } else {
        const { error } = await supabase
          .from('products')
          .update(product)
          .eq('id', params.id)
        if (error) throw error
      }
      router.push('/admin/products')
    } catch (error) {
      console.error('Save error:', error)
      alert('Failed to save')
    } finally {
      setSaving(false)
    }
  }

  const updateTranslation = (locale: string, field: string, value: string) => {
    setProduct({
      ...product,
      translations: {
        ...product.translations,
        [locale]: {
          ...product.translations[locale],
          [field]: value,
        },
      },
    })
  }

  if (loading) return <div>{t('loading')}</div>

  return (
    <div className="max-w-4xl">
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-bold">
          {params.id === 'new' ? t('products_page.add') : t('products_page.edit')}
        </h1>
        <div className="flex gap-2">
          <Button variant="outline" onClick={() => router.back()}>{t('cancel')}</Button>
          <Button onClick={handleSave} disabled={saving}>
            {saving ? t('saving') : t('save')}
          </Button>
        </div>
      </div>

      <div className="space-y-6">
        <Card>
          <CardHeader>
            <CardTitle>{t('basicInfo')}</CardTitle>
          </CardHeader>
          <CardContent className="space-y-4">
            <div className="grid grid-cols-2 gap-4">
              <div>
                <Label>{t('model')}</Label>
                <Input
                  value={product.model}
                  onChange={(e) => setProduct({ ...product, model: e.target.value })}
                />
              </div>
              <div>
                <Label>Slug</Label>
                <Input
                  value={product.slug}
                  onChange={(e) => setProduct({ ...product, slug: e.target.value })}
                />
              </div>
            </div>
            <div className="flex gap-6">
              <div className="flex items-center gap-2">
                <Switch
                  checked={product.published}
                  onCheckedChange={(v) => setProduct({ ...product, published: v })}
                />
                <Label>{t('published')}</Label>
              </div>
              <div className="flex items-center gap-2">
                <Switch
                  checked={product.featured}
                  onCheckedChange={(v) => setProduct({ ...product, featured: v })}
                />
                <Label>{t('featured')}</Label>
              </div>
              <div className="flex items-center gap-2">
                <Switch
                  checked={product.compliance_flag}
                  onCheckedChange={(v) => setProduct({ ...product, compliance_flag: v })}
                />
                <Label>{t('complianceRequired')}</Label>
              </div>
            </div>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <CardTitle>{t('images')}</CardTitle>
          </CardHeader>
          <CardContent>
            <ImageUpload
              images={product.images || []}
              onChange={(images) => setProduct({ ...product, images })}
            />
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <CardTitle>{t('translations')}</CardTitle>
          </CardHeader>
          <CardContent>
            <TranslationTabs
              translations={product.translations || {}}
              fields={TRANSLATION_FIELDS}
              onChange={updateTranslation}
            />
          </CardContent>
        </Card>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 验证修改**

检查文件修改正确，无语法错误。

---

## Task 10: 修改案例研究页面

**Files:**
- Modify: `src/app/admin/case-studies/page.tsx`

- [ ] **Step 1: 修改案例研究页面**

修改 `src/app/admin/case-studies/page.tsx`：

```typescript
'use client'

import { supabaseAdmin } from '@/lib/supabase/admin'
import { DataTable } from '@/components/admin/data-table'
import { Badge } from '@/components/ui/badge'
import Link from 'next/link'
import { buttonVariants } from '@/components/ui/button'
import { Plus } from 'lucide-react'
import { useAdminTranslations } from '@/hooks/use-admin-translations'

export default function CaseStudiesPage() {
  const t = useAdminTranslations()

  const columns = [
    { key: 'created_at', label: t('date'), render: (item: any) => new Date(item.created_at).toLocaleDateString() },
    { key: 'title', label: t('title_field') },
    { key: 'category', label: t('category') },
    {
      key: 'published',
      label: t('status'),
      render: (item: any) => (
        <Badge variant={item.published ? 'default' : 'secondary'}>
          {item.published ? t('published') : t('draft')}
        </Badge>
      )
    },
  ]

  return (
    <div>
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-bold">{t('case_studies_page.title')}</h1>
        <Link href="/admin/case-studies/new" className={buttonVariants()}>
          <Plus className="w-4 h-4 mr-2" />
          {t('case_studies_page.add')}
        </Link>
      </div>
      <CaseStudiesTable columns={columns} />
    </div>
  )
}

async function CaseStudiesTable({ columns }: { columns: any[] }) {
  const { data: caseStudies } = await supabaseAdmin
    .from('case_studies')
    .select('*')
    .order('created_at', { ascending: false })

  const t = useAdminTranslations()

  return (
    <DataTable
      data={caseStudies || []}
      columns={columns}
      searchPlaceholder={t('case_studies_page.searchPlaceholder')}
    />
  )
}
```

- [ ] **Step 2: 验证修改**

检查文件修改正确，无语法错误。

---

## Task 11: 修改解决方案页面

**Files:**
- Modify: `src/app/admin/solutions/page.tsx`

- [ ] **Step 1: 修改解决方案页面**

修改 `src/app/admin/solutions/page.tsx`：

```typescript
'use client'

import { supabaseAdmin } from '@/lib/supabase/admin'
import { DataTable } from '@/components/admin/data-table'
import { Badge } from '@/components/ui/badge'
import Link from 'next/link'
import { buttonVariants } from '@/components/ui/button'
import { Plus } from 'lucide-react'
import { useAdminTranslations } from '@/hooks/use-admin-translations'

export default function SolutionsPage() {
  const t = useAdminTranslations()

  const columns = [
    { key: 'title', label: t('title_field') },
    { key: 'category', label: t('category') },
    {
      key: 'published',
      label: t('status'),
      render: (item: any) => (
        <Badge variant={item.published ? 'default' : 'secondary'}>
          {item.published ? t('published') : t('draft')}
        </Badge>
      )
    },
  ]

  return (
    <div>
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-2xl font-bold">{t('solutions_page.title')}</h1>
        <Link href="/admin/solutions/new" className={buttonVariants()}>
          <Plus className="w-4 h-4 mr-2" />
          {t('solutions_page.add')}
        </Link>
      </div>
      <SolutionsTable columns={columns} />
    </div>
  )
}

async function SolutionsTable({ columns }: { columns: any[] }) {
  const { data: solutions } = await supabaseAdmin
    .from('solutions')
    .select('*')
    .order('sort_order')
    .order('created_at', { ascending: false })

  const t = useAdminTranslations()

  return (
    <DataTable
      data={solutions || []}
      columns={columns}
      searchPlaceholder={t('solutions_page.searchPlaceholder')}
    />
  )
}
```

- [ ] **Step 2: 验证修改**

检查文件修改正确，无语法错误。

---

## Task 12: 测试语言切换功能

- [ ] **Step 1: 启动开发服务器**

运行开发服务器：

```bash
npm run dev
```

- [ ] **Step 2: 访问后台管理**

访问 `http://localhost:3000/admin`，登录后台管理系统。

- [ ] **Step 3: 测试语言切换**

1. 点击侧边栏底部的语言切换按钮（English / 中文）
2. 验证所有导航项立即切换为对应语言
3. 验证页面标题、按钮等界面元素切换为对应语言
4. 刷新页面，验证语言偏好保持

- [ ] **Step 4: 测试各页面翻译**

访问以下页面，验证翻译正确：
- Dashboard
- Products
- Case Studies
- Solutions
- Inquiries

---

## Task 13: 提交代码

- [ ] **Step 1: 检查修改**

运行 git status 查看所有修改：

```bash
git status
```

- [ ] **Step 2: 添加文件**

添加所有修改的文件：

```bash
git add src/components/admin/language-provider.tsx
git add src/components/admin/language-switcher.tsx
git add src/hooks/use-admin-translations.ts
git add src/app/admin/layout.tsx
git add src/components/admin/sidebar.tsx
git add src/app/admin/page.tsx
git add src/app/admin/products/page.tsx
git add src/app/admin/products/[id]/page.tsx
git add src/app/admin/case-studies/page.tsx
git add src/app/admin/solutions/page.tsx
git add messages/en/admin.json
git add messages/zh/admin.json
```

- [ ] **Step 3: 提交更改**

提交所有更改：

```bash
git commit -m "feat: add i18n support for admin panel

- Add AdminLanguageProvider for language state management
- Add AdminLanguageSwitcher component
- Add useAdminTranslations hook
- Extend translation files with detailed keys
- Translate all admin pages (Dashboard, Products, Case Studies, Solutions)
- Support language preference persistence via localStorage"
```

---

## 验收标准

- ✅ 用户可以在后台任意页面切换中英文
- ✅ 语言切换后所有界面元素显示对应语言
- ✅ 刷新页面后语言偏好保持
- ✅ 业务数据的多语言编辑功能不受影响
- ✅ 不影响现有路由结构和认证逻辑
- ✅ 性能无明显下降
